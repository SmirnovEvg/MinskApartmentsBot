name: Minsk-Apartments-Bot

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: files
        run: ls -la
      - name: Container build
        run: docker build -t smirnoveug/minsk-apartments-bot .
      - name: Container push
        run: docker push smirnoveug/minsk-apartments-bot:latest
